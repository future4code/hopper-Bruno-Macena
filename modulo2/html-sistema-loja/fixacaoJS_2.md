```JS
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
const valorDuzia = 1.00
const valorUnidade = 1.30

if(quantidade >= 12){
return valorDuzia*quantidade
}else if (quantidade < 12){
return valorUnidade*quantidade
}}
```